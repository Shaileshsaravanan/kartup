<!DOCTYPE html>
html(lang="en")
head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Kartup - Room Full
    link(rel="stylesheet", href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=M+PLUS+Rounded+1c:wght@500&display=swap")
    style.
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'M PLUS Rounded 1c', sans-serif;
        background-color: #cdeaff;
        background-image: linear-gradient(45deg, rgba(255,255,255,0.05) 25%, transparent 25%, transparent 50%, rgba(255,255,255,0.05) 50%, rgba(255,255,255,0.05) 75%, transparent 75%, transparent);
        background-size: 40px 40px;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #222;
      }

      .full-container {
        background: #fffdf9;
        border: 3px solid #dedede;
        border-radius: 16px;
        padding: 40px;
        text-align: center;
        box-shadow: 0 6px 0 #bcbcbc;
        max-width: 500px;
        width: 90%;
      }

      .full-icon {
        font-size: 3rem;
        margin-bottom: 20px;
      }

      h1 {
        font-family: 'Press Start 2P', cursive;
        font-size: 1.2rem;
        color: #e60012;
        margin-bottom: 15px;
      }

      p {
        font-size: 1rem;
        margin-bottom: 30px;
        color: #333;
      }

      .btn {
        padding: 14px 28px;
        border: none;
        border-radius: 12px;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        background-color: #0065d9;
        color: white;
        box-shadow: 0 4px 0 #0047a4;
        transition: transform 0.2s ease;
      }

      .btn:hover {
        transform: translateY(-2px);
      }

body
    .full-container
        .full-icon 🚗💨
        h1 Room Full!
        p This room already has 2 players. Please try creating a new room or joining a different one.
        a.btn(href="/") Back to Lobby